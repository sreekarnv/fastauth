# Google OAuth Credentials
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URI=http://localhost:8000/oauth/google/callback

# JWT Secret Key (also used for session encryption)
# Generate a strong secret: openssl rand -hex 32
# IMPORTANT: Keep this secret and never commit it to version control!
JWT_SECRET_KEY=CHANGE_ME_IN_PRODUCTION_USE_STRONG_SECRET

# Database
DATABASE_URL=sqlite:///./oauth_example.db

# Email (using console for development)
EMAIL_BACKEND=console
REQUIRE_EMAIL_VERIFICATION=false
